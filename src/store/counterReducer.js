const initialState = 0

const counterReducer = (state = initialState, action) => {
  switch (action.type) {
    // Обработка действий
    default:
      return state
  }
}

export default counterReducer
